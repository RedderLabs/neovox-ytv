name: neovox
description: "NEOVOX Music — Modern music streaming app"
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  youtube_explode_dart: ^3.0.5
  just_audio: ^0.10.5
  audio_service: ^0.18.18
  dio: ^5.9.2
  cached_network_image: ^3.4.1
  shared_preferences: ^2.5.5
  google_fonts: ^6.2.1
  palette_generator: ^0.3.3+4
  shimmer: ^3.0.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
